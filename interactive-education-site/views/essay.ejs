<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Essay Question</title>
  <!-- Include Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <!-- Include the header partial -->
  <%- include('partials/header') %>

  <div class="container mt-5">
    <h1 class="mb-4">Essay Questions</h1>

    <form action="/essay" method="POST">
      <div class="mb-4">
        <label class="form-label"> Briefly explain what is analogue data?</label>
        <textarea name="q1" class="form-control" rows="4"></textarea>
      </div>

      <div class="mb-4">
        <label class="form-label">What is the difference between analogue data and digital data?</label>
        <textarea name="q2" class="form-control" rows="4"></textarea>
      </div>

      <div class="mb-4">
        <label class="form-label">What are the two internal memories? Explain briefly each of them.</label>
        <textarea name="q3" class="form-control" rows="4"></textarea>
      </div>

      <div class="mb-4">
        <label class="form-label">Compare and contrast between the command line interface (CLI) and graphical user interface (GUI).</label>
        <textarea name="q4" class="form-control" rows="4"></textarea>
      </div>

      <div class="mb-4">
        <label class="form-label">What are direct data entry (DDE) devices? Give one example of direct data entry device. State its uses, advantages and disadvantages.</label>
        <textarea name="q5" class="form-control" rows="4"></textarea>
      </div>

      <!-- Submit Button -->
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </div>

   <!-- Include the footer partial -->
   <%- include('partials/footer') %>

  <!-- Include Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
