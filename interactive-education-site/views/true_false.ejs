<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>True/False Quiz</title>
  <!-- Include Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <!-- Include the header partial -->
  <%- include('partials/header') %>

  <div class="container mt-5">
    <h1 class="mb-4">True/False Quiz</h1>

    <% if (typeof feedback === 'undefined') { %> 
      <!-- Quiz Form (Only show if no feedback exists, i.e. before submission) -->
      <form action="/true_false" method="POST">
        <!-- True/False Question 1 -->
        <div class="mb-4">
          <label class="form-label">Question 1: Changes to data types or other properties can be made from the Database Tools tab.</label>
          <div class="form-check">
            <input class="form-check-input" type="radio" name="Q1" value="True" id="q1a1">
            <label class="form-check-label" for="q1a1">True</label>
          </div>
          <div class="form-check">
            <input class="form-check-input" type="radio" name="Q1" value="False" id="q1a2">
            <label class="form-check-label" for="q1a2">False</label>
          </div>
        </div>

        <!-- True/False Question 2 -->
        <div class="mb-4">
            <label class="form-label">Question 2: To import a file for a task, we have to select External Data tab.</label>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="Q2" value="True" id="q2a1">
              <label class="form-check-label" for="q2a1">True</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="Q2" value="False" id="q2a2">
              <label class="form-check-label" for="q2a2">False</label>
            </div>
        </div>

         <!-- True/False Question 3 -->
         <div class="mb-4">
            <label class="form-label">Question 3: Select Delimited in the Import Text Wizard window for the .csv files. </label>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="Q3" value="True" id="q3a1">
              <label class="form-check-label" for="q3a1">True</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="Q3" value="False" id="q3a2">
              <label class="form-check-label" for="q3a2">False</label>
            </div>
        </div>

        <!-- True/False Question 4 -->
        <div class="mb-4">
            <label class="form-label">Question 4: To change Price field into a numeric field with a currency sub-type, click on Data Type cell and use the drop-down list to select the Integer data type.</label>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="Q4" value="True" id="q4a1">
              <label class="form-check-label" for="q4a1">True</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="Q4" value="False" id="q4a2">
              <label class="form-check-label" for="q4a2">False</label>
            </div>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>

    <% } else { %> 
      <!-- Quiz Results (Only show after submission) -->
      <h2 class="mt-5">Quiz Results</h2>

      <% for (let question in feedback) { %>
        <div class="mb-3">
          <p><strong>Question:</strong> <%= question %></p>
          <p><strong>Your Answer:</strong> <%= feedback[question] === 'correct' ? 'Correct' : 'Wrong' %></p>
          <p><strong>Correct Answer:</strong> <%= correctAnswerDisplay[question] %></p>
        </div>
      <% } %>

      <p class="mt-3"><strong>Your Score:</strong> <%= score %>/4</p>

    <% } %> <!-- Close the else block -->

  </div>

  <!-- Include the footer partial -->
  <%- include('partials/footer') %>

  <!-- Include Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
